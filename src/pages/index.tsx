import Layout from "../components/Layout";
import VerifyProof from "../components/VerifyProof";

const Home: React.FC = () => {
  return (
    <Layout>
      <VerifyProof />
    </Layout>
  );
};

export default Home;
